# openlibrary_rtbdp_project

For running you need to write in terminal

docker compose up -d 

And then you can access to different things in browser:
Grafana: http://localhost:23000
Kafka UI: http://localhost:28080
pgAdmin: http://localhost:20080
